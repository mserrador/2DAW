//querySelectorAll
//devuelve un array
//devuelve todos los elementos que encuentra
//si no encuentra nada, devuelve un array vacio

const card=document.querySelectorAll('.card');
console.log(card);

const formu=document.querySelectorAll('#formulario');
console.log(formu);